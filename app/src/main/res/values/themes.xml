<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClinicApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/green_primary</item>
        <item name="android:windowBackground">@color/mint</item>
    </style>

    <style name="Theme.ClinicApp" parent="Base.Theme.ClinicApp" />
    <style name="CustomChip" parent="Widget.Material3.ChipGroup">
        <item name="android:textColor">@color/custom_text_chip_color</item>
        <item name="chipBackgroundColor">@color/custom_chip_color</item>
        <item name="chipStrokeColor">@color/custom_chip_outline</item>
        <item name="chipStrokeWidth">1dp</item>
    </style>
</resources>